<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
    <div class="container-fluid">
      <a class="navbar-brand" href="/">Home</a>
        <ul class="navbar-nav">
            <li class="nav-item">
                <a class="nav-link text-light" href="/articles/new">Create</a>
            </li>
            <li class="nav-item">
                <a class="nav-link text-light" href="/list">Posts</a>
            </li>
            <li class="nav-item">
                <% if(user) { %>
                    <a class="nav-link text-light" href="/profile">Profile</a>
                    <% } %>
            </li>
            <li class="nav-item">
                <% if(user) { %>
                    <a class="btn btn-success nav-link text-light" href="/logout">Log Out</a>  
                <% } else { %>
                    <a class="btn btn-success nav-link text-light" href="/login">Log In</a>
                <% }  %>
            </li>
        </ul>
    </div>
</nav>